spring:
   devtools:
     restart:
       enabled: false
   datasource:
       url: jdbc:mysql://localhost:3306/db_soft1921?useUnicode=true&characterEncoding=utf-8&useSSL=false
       hikari: # 整合hikari （java数据库连接池）
       username: root
       password: 123
       driver-class-name: com.mysql.jdbc.Driver

   jpa:
       hibernate:
         ddl-auto: update #建表create  后面用update
       show-sql: true
       open-in-view: false
